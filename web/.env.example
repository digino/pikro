# Copy this file to .env.local and adjust as needed.
# .env.local is gitignored — never commit real values.

# When "true", the frontend uses mock API data instead of talking to the Go backend.
# Useful for UI development without a running router. Default: false.
VITE_MOCK=false
